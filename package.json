{
  "name": "teachtimer-io",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @teachtimer/web",
    "build": "npm run build --workspace @teachtimer/web",
    "lint": "npm run lint --workspace @teachtimer/web",
    "typecheck": "npm run typecheck --workspace @teachtimer/web"
  }
}
